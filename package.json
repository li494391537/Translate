{
  "name": "translate",
  "version": "1.0.0",
  "description": "A translate application",
  "main": "main.js",
  "scripts": {
    "init": "node -e \"require('fs').exists('settings.yml', (exists)=>{if(!exists) require('fs').writeFile('settings.yml', 'appid: \\nappkey: \\n',(err) => {});});\"",
    "start": "node main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "lirui",
  "license": "MIT",
  "dependencies": {
    "js-yaml": "^3.12.0",
    "urlencode": "^1.1.0"
  }
}